<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@color/gote_light_green"
    android:padding="4dp">

    <!-- Hole number -->
    <TextView
        android:id="@+id/tvHoleNumber"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textColor="@color/gote_dark_green"
        android:gravity="center"
        android:text="1" />

    <!-- Distance -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilDistance"
        android:layout_width="64dp"
        android:layout_height="wrap_content"
        android:hint="Dist"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense"
        android:layout_marginStart="4dp">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etDistance"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="number"
            android:maxLines="1" />

    </com.google.android.material.textfield.TextInputLayout>

    <!-- Par -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilPar"
        android:layout_width="56dp"
        android:layout_height="wrap_content"
        android:hint="Par"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense"
        android:layout_marginStart="4dp">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etPar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="number"
            android:maxLines="1" />

    </com.google.android.material.textfield.TextInputLayout>

    <!-- Player scores injected here dynamically -->
    <LinearLayout
        android:id="@+id/playerScoresContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="horizontal" />

    <!-- Delete button -->
    <ImageButton
        android:id="@+id/btnDeleteHole"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@android:drawable/ic_menu_delete"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Delete hole" />

</LinearLayout>